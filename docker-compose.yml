version: '3.8'

services:
  backend:
    build: ./backend
    container_name: risk-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: risk-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend